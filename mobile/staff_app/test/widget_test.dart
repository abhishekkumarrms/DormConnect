import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test placeholder', (tester) async {
    // Staff app requires Firebase + ProviderScope to run — skip widget smoke test.
    expect(true, isTrue);
  });
}
